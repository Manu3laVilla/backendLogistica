# Configuracion de log
logging:
  level:
    org:
      hibernate: ERROR
      springframework: INFO
    root: INFO
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} {%thread} {%-5level} {%c{l}}: %msg%n'
server:
  error:
    include-stacktrace: never

# Configuracion de conexion a la BD
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: mnzihofnhrslefb6
    url: jdbc:mysql://mnzihofnhrslefb6:cbb2jb9an1y22min@u6354r3es4optspf.cbetxkdyhwsb.us-east-1.rds.amazonaws.com:3306/u1i328swowvt5pbz
    password: cbb2jb9an1y22min
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: update

# Configuracion de seguridad
jwt:
  security: false
  secret: secret
  expiration: 3600
